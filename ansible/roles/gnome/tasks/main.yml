---
# GNOME設定
# 注意: 入力ソース切り替え (Ctrl+Space) は fcitx5 ロールで管理

- name: Set GNOME input sources
  command: >
    gsettings set org.gnome.desktop.input-sources sources "[('xkb', 'us')]"
  become: false
  changed_when: false
